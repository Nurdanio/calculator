import { createStyles } from "@mantine/core";

export const useButtonStyles = createStyles(() => ({
  center: {
    width: 72,
    height: 72,
    borderRadius: 24,
    color: "#FFFFFF",
    fontSize: 32,
  },
}));
