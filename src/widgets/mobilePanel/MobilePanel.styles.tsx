import { createStyles } from "@mantine/core";

export const useMobilePanelStyles = createStyles(() => ({
  flex: {
    width: "100%",
    justifyContent: "space-between",
    alignItems: "center",
    color: "#FFFFFF",
  },
}));
